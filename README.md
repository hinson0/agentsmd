# AGENTS.md

一份个人 AI 编码代理协作约定，覆盖回答风格、判断原则和交流语气。

规则正文见 [AGENTS.md](./AGENTS.md)，并以该文件为唯一准则。

## 使用

将 `AGENTS.md` 放到目标项目中，让支持该文件的编码代理在工作前读取它。

```bash
cp AGENTS.md /path/to/project/AGENTS.md
```

项目需要额外约束时，直接在目标项目的 `AGENTS.md` 中补充；不要在 README 中复制规则，免得两份内容悄悄长歪。
